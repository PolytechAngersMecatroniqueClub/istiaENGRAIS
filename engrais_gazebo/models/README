To generate the urdf file 
    rosrun xacro xacro --inorder -o ouputfile.sdf inputfile.xacro

To check the xacro file syntax
    xacro inputfile.xacro | check_urdfs

Using the makefile:
    make green
    make red
    make all
